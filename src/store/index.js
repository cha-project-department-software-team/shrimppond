// Import store từ file store.js
import store from './store'; // Giả sử bạn đã tạo file store.js

// Nếu bạn có thêm các slice hoặc reducer khác, import và export chúng ở đây
// Ví dụ: import { increment, decrement } from './counterSlice';

// Export store và các thành phần khác (nếu có)
export default store ;
